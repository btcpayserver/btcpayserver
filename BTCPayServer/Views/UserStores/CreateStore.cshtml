@model BTCPayServer.Models.StoreViewModels.CreateStoreViewModel
@{
    ViewData.SetActivePageAndTitle(StoreNavPages.Create, "Create a new store");
}

<section>
    <div class="container">
        <partial name="_StatusMessage" />

        <h2 class="mb-4">@ViewData["PageTitle"]</h2>
        <div class="row">
            <div class="col-lg-6">
                <form asp-action="CreateStore">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <div class="form-group">
                        <label asp-for="Name" class="control-label" data-required></label>
                        <input asp-for="Name" class="form-control" required />
                        <span asp-validation-for="Name" class="text-danger"></span>
                    </div>
                    <div class="form-group">
                        <input type="submit" value="Create" class="btn btn-primary" id="Create" />
                        <a asp-action="ListStores" class="text-muted ml-3">Back to list</a>
                    </div>
                </form>
            </div>
        </div>
    </div>
</section>
